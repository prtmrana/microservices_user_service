User services :


It has all the operations which is used related to User :

like : all CRUD operations related to user 

and it has also integrated with redis caching so that data can be cache realted to user.
